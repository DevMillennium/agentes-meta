services:
  postgres:
    image: postgres:16-alpine
    container_name: phoenix-marketing-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: phoenix_marketing_ai
    ports:
      - "5432:5432"
    volumes:
      - phoenix_pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: phoenix-marketing-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  phoenix_pg_data:
